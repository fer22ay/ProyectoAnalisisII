<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.umg.analisis">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoAnalisis">
        <activity
            android:name=".view.ui.activities.MainActivity"
            android:exported="true" />
        <!-- API de Google para el uso de mapas Splashscreen-->

        <activity
            android:name=".view.ui.activities.Splashscreen"
            android:exported="true"
            android:theme="@style/ActivitySplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>